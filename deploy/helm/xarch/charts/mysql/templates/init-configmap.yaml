apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mysql.fullname" . }}-init
  labels:
    {{- include "mysql.labels" . | nindent 4 }}
data:
{{- range $name, $script := .Values.initScripts }}
  {{ $name }}: |
{{ $script | indent 4 }}
{{- end }}
