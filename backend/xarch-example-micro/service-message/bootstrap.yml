# service-message/bootstrap.yml

spring:
  application:
    name: xarch-service-message
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:nacos.xarch:8848}
        namespace: xarch-cloud
      config:
        server-addr: ${NACOS_SERVER:nacos.xarch:8848}
        namespace: xarch-cloud
        file-extension: yaml
        refresh-enabled: true