<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xarch.cms.mapper.CommentMapper">

    <resultMap id="CommentResult" type="com.xarch.cms.entity.Comment">
        <id property="id" column="id"/>
        <result property="articleId" column="article_id"/>
        <result property="userId" column="user_id"/>
        <result property="content" column="content"/>
        <result property="parentId" column="parent_id"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectByArticleId" resultMap="CommentResult">
        SELECT id, article_id, user_id, content, parent_id, status, create_time
        FROM cms_comment
        WHERE article_id = #{articleId}
          AND is_deleted = 0
          AND status = 'VISIBLE'
        ORDER BY create_time ASC
    </select>

    <select id="countByArticleId" resultType="long">
        SELECT COUNT(*)
        FROM cms_comment
        WHERE article_id = #{articleId}
          AND is_deleted = 0
          AND status = 'VISIBLE'
    </select>

</mapper>
